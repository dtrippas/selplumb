version: '3'

services:
    selenium-chrome:
        image: selenium/standalone-chrome
        ports: 
            - 4444:4444

    r-tidyverse: 
        image: dtrippas/tidyverse-plus:v0.0.3 
        ports:
            - 8787:8787
            - 80:80
        environment:
            - PASSWORD=12345678
        volumes:
            - ./:/home/rstudio
